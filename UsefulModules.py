import numpy as np								#imports numpy library
import matplotlib.pyplot as plt		#imports Matplotlib pyplot

import sys												#grants sys library access
import os 												#grants access to operating system

print(sys.argv)										#prints any command line arguments, including program name
print(os.getcwd())								#prints current working directory

import os

help(os)
